/*
package com.revature.cookingvolumetoweight;

import android.os.Bundle;

import androidx.fragment.app.Fragment;
import androidx.navigation.NavController;

import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;


*/
/**
 * A simple {@link Fragment} subclass.
 *//*

public class ComboFragment extends Fragment {

    private DisplayFragment displayFragment;
    private CalculatorFragment calculatorFragment;
    private NavController navController;

    public ComboFragment() {
        // Required empty public constructor
    }


    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container,
                             Bundle savedInstanceState) {
        // Inflate the layout for this fragment
        View view = inflater.inflate(R.layout.fragment_combo, container, false);

        displayFragment = (DisplayFragment) getChildFragmentManager().findFragmentById(R.id.display_fragment);
        calculatorFragment = (CalculatorFragment) getChildFragmentManager().findFragmentById(R.id.calculator_fragment);

        return view;
    }

    public void updateTextView(String str){
        displayFragment.updateEntry(str);
    }
}
*/
